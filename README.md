# DAC
Proof of *Doc as Code* concept.

github auto-previews check list:
- `.md` by default
- `.adoc` by default

- `.png` by default
- `.svg` by default
- `.html` by default
- `.pdf` by default
  
...